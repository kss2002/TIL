## 특정한 크기에서의 반응형 구현

반응형에서, 기본값은 테일윈드가 세팅을 이미 해두었다. 세팅값은 다음과 같다.

```
Breakpoint prefix Minimum width CSS
sm | 40rem (640px) | @media (width >= 40rem) { ... }
md | 48rem (768px) | @media (width >= 48rem) { ... }
lg | 64rem (1024px) | @media (width >= 64rem) { ... }
xl | 80rem (1280px) | @media (width >= 80rem) { ... }
2xl | 96rem (1536px) | @media (width >= 96rem) { ... }
```

Tailwind CSS에서 커스텀 브레이크포인트를 사용하려면 tailwind.config.js에서 설정해야 합니다.

방법 1: tailwind.config.js에 커스텀 브레이크포인트 추가

```js
// tailwind.config.js
export default {
  theme: {
    screens: {
      xs: '387px', // 커스텀 브레이크포인트
      sm: '640px',
      md: '768px',
      lg: '1024px',
      xl: '1280px',
      '2xl': '1536px',
    },
  },
};
```

그러면 이렇게 사용할 수 있어요:

```jsx
<h1 className="text-3xl xs:text-5xl md:text-7xl ...">
```

- 기본(387px 미만): text-3xl
- 387px 이상: text-5xl
- 768px 이상: text-7xl

방법 2: 임의 값(Arbitrary Value) 사용

config 수정 없이 바로 사용하고 싶다면:

```jsx
<h1 className="text-3xl min-[387px]:text-5xl md:text-7xl ...">
```

min-[387px]: 이렇게 대괄호 안에 원하는 픽셀 값을 넣으면 됩니다!

참고: Tailwind는 mobile-first이기 때문에, 가장 작은 사이즈를 기본값으로 두고 점점 큰 화면에서 크기를 키우는 방식으로 작성하는 게 일반적입니다! 👍
