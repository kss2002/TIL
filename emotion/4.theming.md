# Emotion Theming (테마 시스템)

> 일관된 디자인 시스템을 위한 테마 관리

## 왜 테마가 필요한가?

- **일관성**: 컬러, 스페이싱, 폰트 등을 한 곳에서 관리
- **유지보수**: 디자인 변경 시 테마만 수정하면 됨
- **다크모드**: 테마 전환을 쉽게 구현 가능

## 기본 설정

### 1. 테마 정의

```jsx
// theme.js
const theme = {
  colors: {
    primary: '#3498db',
    secondary: '#2ecc71',
    danger: '#e74c3c',
    background: '#f5f5f5',
    text: '#333333',
  },
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '16px',
    lg: '24px',
    xl: '32px',
  },
  fontSizes: {
    small: '12px',
    medium: '16px',
    large: '20px',
    xlarge: '24px',
  },
  borderRadius: {
    small: '4px',
    medium: '8px',
    large: '16px',
  },
};

export default theme;
```

### 2. ThemeProvider 설정

```jsx
// App.jsx
import { ThemeProvider } from '@emotion/react';
import theme from './theme';

function App() {
  return (
    <ThemeProvider theme={theme}>
      <MainContent />
    </ThemeProvider>
  );
}
```

## 테마 사용하기

### styled 컴포넌트에서 사용

```jsx
import styled from '@emotion/styled';

const Button = styled.button`
  background-color: ${(props) => props.theme.colors.primary};
  color: white;
  padding: ${(props) => props.theme.spacing.sm}
    ${(props) => props.theme.spacing.md};
  border-radius: ${(props) => props.theme.borderRadius.small};
  font-size: ${(props) => props.theme.fontSizes.medium};
  border: none;
  cursor: pointer;

  &:hover {
    opacity: 0.9;
  }
`;

const Card = styled.div`
  background: white;
  padding: ${({ theme }) => theme.spacing.lg};
  border-radius: ${({ theme }) => theme.borderRadius.medium};
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
`;
```

### css prop에서 사용 (useTheme)

```jsx
/** @jsxImportSource @emotion/react */
import { css, useTheme } from '@emotion/react';

function Alert({ message }) {
  const theme = useTheme();

  return (
    <div
      css={css`
        background-color: ${theme.colors.danger};
        color: white;
        padding: ${theme.spacing.md};
        border-radius: ${theme.borderRadius.small};
      `}
    >
      {message}
    </div>
  );
}
```

## 다크모드 구현

### 1. 라이트/다크 테마 정의

```jsx
// theme.js
const baseTheme = {
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '16px',
    lg: '24px',
  },
  borderRadius: {
    small: '4px',
    medium: '8px',
  },
};

export const lightTheme = {
  ...baseTheme,
  colors: {
    primary: '#3498db',
    background: '#ffffff',
    surface: '#f5f5f5',
    text: '#333333',
    textSecondary: '#666666',
  },
};

export const darkTheme = {
  ...baseTheme,
  colors: {
    primary: '#5dade2',
    background: '#1a1a2e',
    surface: '#16213e',
    text: '#ffffff',
    textSecondary: '#b0b0b0',
  },
};
```

### 2. 테마 전환 구현

```jsx
import { useState } from 'react';
import { ThemeProvider } from '@emotion/react';
import { lightTheme, darkTheme } from './theme';

function App() {
  const [isDark, setIsDark] = useState(false);

  const toggleTheme = () => {
    setIsDark((prev) => !prev);
  };

  return (
    <ThemeProvider theme={isDark ? darkTheme : lightTheme}>
      <div>
        <button onClick={toggleTheme}>
          {isDark ? '🌞 Light Mode' : '🌙 Dark Mode'}
        </button>
        <MainContent />
      </div>
    </ThemeProvider>
  );
}
```

### 3. 시스템 설정 감지

```jsx
import { useEffect, useState } from 'react';

function usePrefersDarkMode() {
  const [prefersDark, setPrefersDark] = useState(
    window.matchMedia('(prefers-color-scheme: dark)').matches,
  );

  useEffect(() => {
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    const handler = (e) => setPrefersDark(e.matches);

    mediaQuery.addEventListener('change', handler);
    return () => mediaQuery.removeEventListener('change', handler);
  }, []);

  return prefersDark;
}
```

## TypeScript 타입 정의

```tsx
// emotion.d.ts
import '@emotion/react';

declare module '@emotion/react' {
  export interface Theme {
    colors: {
      primary: string;
      secondary: string;
      background: string;
      text: string;
    };
    spacing: {
      xs: string;
      sm: string;
      md: string;
      lg: string;
    };
  }
}
```

## 테마 네스팅

특정 영역에서 테마를 오버라이드할 수 있다:

```jsx
import { ThemeProvider } from '@emotion/react';

const mainTheme = {
  colors: { primary: '#3498db' },
};

const sidebarTheme = {
  colors: { primary: '#e74c3c' },
};

function App() {
  return (
    <ThemeProvider theme={mainTheme}>
      <MainContent />
      <ThemeProvider theme={sidebarTheme}>
        <Sidebar />
      </ThemeProvider>
    </ThemeProvider>
  );
}
```

## 베스트 프랙티스

1. **테마 구조 일관성 유지**: 라이트/다크 테마의 구조를 동일하게 유지
2. **시맨틱 네이밍**: `blue` 대신 `primary`, `gray` 대신 `textSecondary` 사용
3. **테마 타입 정의**: TypeScript 사용 시 테마 타입을 반드시 정의
4. **테마 확장 시 spread 연산자 활용**: 기본 테마를 확장할 때 유용
